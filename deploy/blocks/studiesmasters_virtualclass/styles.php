<?php
// blocks/studiesmasters_virtualclass/styles.php
// Scopes the block CSS to the block only. The block previously emitted
// .sm-dashboard-* classes with no stylesheet anywhere, so it rendered as
// unstyled raw HTML on the dashboard.
defined('MOODLE_INTERNAL') || die();

$plugin->requires = 2020061500;
$plugin->contentwidth = 400;
$plugin->dependencies = array();